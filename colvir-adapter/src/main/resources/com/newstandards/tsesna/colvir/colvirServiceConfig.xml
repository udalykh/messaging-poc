<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/integration"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:context="http://www.springframework.org/schema/context"
             xmlns:jms="http://www.springframework.org/schema/integration/jms"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
			http://www.springframework.org/schema/beans/spring-beans.xsd
			http://www.springframework.org/schema/context
			http://www.springframework.org/schema/context/spring-context.xsd
			http://www.springframework.org/schema/integration
			http://www.springframework.org/schema/integration/spring-integration.xsd
			http://www.springframework.org/schema/integration/jms
			http://www.springframework.org/schema/integration/jms/spring-integration-jms.xsd">


    <jms:outbound-gateway request-channel="colvirRequestsChannel"
                          request-destination-name="${colvir.requestDestination}"
                          reply-destination-name="${colvir.replyDestination}"
                          correlation-key="JMSCorrelationID"
                          reply-channel="colvirReplyChannel"
                          receive-timeout="${colvir.receiveTimeout}"
                          reply-timeout="${colvir.replyTimeout}">
        <jms:reply-listener/>
    </jms:outbound-gateway>

    <channel id="colvirRequestsChannel" />
    <channel id="colvirReplyChannel"/>
    <channel id="vinCodesChannel"/>
    <channel id="kbkListChannel"/>

    <channel id="demoChannel"/>

    <jms:inbound-gateway request-destination-name="${colvir.requestDestination}"
                         request-channel="demoChannel"/>





<!--    <service-activator input-channel="demoChannel" ref="colvirSoapConnector"/>-->
    <!--


	<jms:inbound-gateway id="jmsin"
	                     request-destination="requestQueue"
	                     request-channel="demoChannel"/>

	<channel id="demoChannel"/>

	<service-activator input-channel="demoChannel" ref="demoBean"/>

    -->

</beans:beans>
